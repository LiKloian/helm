apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lkloiani-chart.fullname" . }}-test-connection"
  labels:
    {{- include "lkloiani-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lkloiani-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
